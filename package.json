{
  "name": "paper-deadlines",
  "version": "1.0.0",
  "description": "DDL Radar - CS conference deadline tracker",
  "private": true,
  "bin": {
    "ddl-radar": "cli/ddl-radar.js"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/build.js --validate",
    "pack:extension": "mkdir -p dist && cd clients/extension && zip -rq ../../dist/ddl-radar-extension.zip . -x '*.DS_Store' && echo 'dist/ddl-radar-extension.zip 已生成'",
    "pack:cli": "cd cli && npm pack --pack-destination ../dist"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "js-yaml": "^4.1.0"
  }
}
